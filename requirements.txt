# Direct dependencies only.
#
# Do NOT regenerate this with `pip freeze` -- on Windows that pulls in
# pywin32 (a locust dependency), which has no Linux wheel and breaks the
# deployment build.

# --- application ---
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
Flask-JWT-Extended==4.7.4
psycopg2-binary==2.9.12
python-dotenv==1.2.2

# --- production servers ---
gunicorn==26.1.0        # deploy target (Linux only -- cannot run on Windows)
waitress==3.0.2         # local production server, works on Windows

# --- testing and load simulation ---
pytest==9.1.1
locust==2.46.3
